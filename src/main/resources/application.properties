spring.application.name=trashmasters


# MongoDB Configuration
# We use an environment variable so your password is never in the code
spring.data.mongodb.database=${MONGO_DB}
spring.data.mongodb.uri=${MONGO_URI}

# Logging
#logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# in order to enforce unique ids, we have to add this
spring.data.mongodb.auto-index-creation=true


# Email (required for JavaMailSender bean)
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=trashmasters.dev@gmail.com
#spring.mail.password=xtogpcieferrgndc
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#
## App properties
#app.frontend.url=http://localhost:5173
#app.mail.from-name=Trash Masters Support
#app.reset.token.expiration=3600000

# === Email Configuration (with .env fallback) ===
# Spring Boot will first check system properties (set by dotenv), then use these defaults
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE:true}

# === App Properties ===
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.mail.from-name=${MAIL_FROM_NAME:Trash Masters Support}
app.reset.token.expiration=3600000